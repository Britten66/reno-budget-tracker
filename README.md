# reno-budget-tracker
A REST API for tracking a home renovation budget across categories like paint, flooring, and extras.


I used a mix of JUnit features intentionally

@BeforeEach to avoid repeating setup
@DisplayName for readable reports 

also adding a parameterized test to check multiple inputs in one method choosing each based on what the test needed.